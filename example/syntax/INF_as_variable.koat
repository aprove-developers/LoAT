# Ensure that INF is valid as variable in the input (not to be confused with INF cost)

(GOAL COMPLEXITY)
(STARTTERM (FUNCTIONSYMBOLS start))
(VAR x INF)
(RULES
  start(x, INF) -> f(x, INF)
  f(x, INF) -> f(x, INF-1) [ INF > 0 ]
)

