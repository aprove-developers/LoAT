# This example shows how free variable can be eliminated using transitive closure

(GOAL COMPLEXITY)
(STARTTERM (FUNCTIONSYMBOLS start))
(VAR x y z)
(RULES
  start(x, y) -> f(x, y)
  f(x, y) -> f(x+1, y) [ x <= z && z < y ]
)
